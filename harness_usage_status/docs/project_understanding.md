WHAT
We need to have a usage aware script that could run to give us a status of remaining quotas for all the model / plan providers (subscription based).
WHY
It would help in:
having a centralized view of quotas availble
deciding model / task routing
Details
Planning to support usage stats (programmatically or with BA (browser automation as a fallback)) for:
- claude code
- codex
- gemini
- minimax
- glm
- qwen

Also would add a status command for it so that uptime for all models can be qeureied and displayed easily in 1 shot.
