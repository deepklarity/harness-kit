# Audit — waves 10+11 close, external-project chunks 6–9

**Verdict: on track — the kit survived its worst night and fixed itself.**
19/19 wave tasks merged and verified in code (12-point artifact check, all
present), full verify gate green, external-project chunks 6–8 merged and verified
live (8 sources healthy, first scrape proved the freshness gate and the new
scrapers). Chunk 9 (cards taste, opus) and the cards-UI check (341, minimax)
are in flight.

## Verified done, with proof
| Claim | Proof |
|---|---|
| Spec gate | cli.py:824, --no-gate present |
| Evidence classifier | harnesses/base.py:368, OUTCOME_UNCONFIRMED |
| Resume-in-worktree | test_truncation_resume_outcome.py |
| Host traces + finish reasons | orchestrator.py:4928 |
| Routing policy engine + UI | failure_policy.py:60, views routing_policy |
| Failed-task attention | failed_reminder.py:54 |
| Ledger auto-answer | fingerprints.py |
| PROJECT_NOTES injection | project_notes.py + seeded file in the external project repo |
| Post-merge gate | worktree.py:1293 |
| Resolver + re-provision | taskit/manager.py:15, config.py:277 |
| Sandbox screenshots | in-guest Chromium (c5b95051 lineage) |
| Roster defaults | views.py:1926 |
| Full gate | scripts/verify.sh exit 0 |

## Score moves (evidence in the table on mission control)
Trust 8→7 · Memory 5→6 · Routing 6→7 · Getting oriented 5→6 · Watching 5→6 ·
Human's seat 5→6 · Project powers 1→5 · Audits 4 hold · Ease 3 hold ·
Runs anywhere 2 hold · Moonshots 2→3.

Trust goes DOWN despite the fixes: the merge agent was caught tonight
committing conflict markers and splicing file versions markerlessly (three
operator git surgeries), and the first master merge of external-project chunk 6 ran
early and silently missed three tasks' work. 338's gate closes part of it;
the splice behavior and stale-base merging are open wave-12 work.

## Top 3 risks
1. Merge agent correctness — it can still silently combine two versions of
   a file wrong. Mitigation: wave-12 item, gate already refuses syntax
   breakage.
2. New machinery unproven live — classifier/resume/policy landed tonight and
   have run once or twice, not a hundred times. Mitigation: watch this
   week's daily runs; stage one deliberate failure.
3. agy exec path crashes on contact (3x tonight). Mitigation: benched by
   routing until a fix task runs.

## Metrics (hand-counted from board history; script still missing — gap)
8 runs lost to the status contract in one night (now structurally fixed);
3 merges needed operator git surgery; 2 hangs under memory pressure at 3
slots; 0 tasks lost silently — every failure surfaced and was dispositioned.
