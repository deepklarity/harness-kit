# Fable roadmap audit — 2026-07-06 (first audit: wave-1 / M1 close-out)

**Verdict: ON TRACK.** The bootstrap loop is real — 18 board-native tasks shipped in
wave 1 (vs. the ≥3 the M1 exit criterion demanded), the full confined pipeline is
proven including in-guest MCP proof and reflection-enforced user directives — but M1
is **not fully exited**: lint/pre-commit is parked (#98), CI's <15-min timing is
unproven, and two test-suite-drift clusters are eroding the verification signal.

## Verified done (proof actually executed 2026-07-06)

| Initiative / task | Claim | Proof run | Result |
|---|---|---|---|
| 01.1 CI pipeline (#97) | repo-wide CI | `.github/workflows/ci.yml` exists (V6); PR #110 | CONFIRMED (timing + red/green evidence still owed — see next actions) |
| 01.3 DAG single-source (#99) | one DAG impl | odin unit suite: `test_dag.py` green in V2 run | CONFIRMED |
| 01.4 subprocess-edge gaps (#100) | TEST_PLAN gaps closed | mock suite largely green; see drift note below | CONFIRMED with caveat |
| 09.1 loop registry (#101) | registry doc | `docs/loops/_REGISTRY.md` exists, correct heading (V8) | CONFIRMED |
| 14.1 ignition | ≥3 tasks via board | board: 18 DONE on spec #49 (V4); spot-checks #103/#114/#119 (V5) | CONFIRMED — exceeded 6× |
| 07.0 microsandbox (partial) | confined execution | 57/57 harness mock tests (V10); agy staged (V11); 15+ confined task runs this wave | CONFIRMED on macOS; **Linux proof outstanding** → stays `[~]` |
| #110–#119 follow-ups | bug-fix wave | live-verified: notifications 200/200 (V3), Timeline renders (browser, 07-06), guardrails reject unknown fields live (V13), merge agent module present (V12), active lineup exact (V9) | CONFIRMED |

## Downgrades and divergences

1. **M1 exit criteria: 3 of 5 met.** Met: DAG single-sourced; harness gaps; ignition.
   Not met: lint+pre-commit enforced (**#98 parked**, `.pre-commit-config.yaml` absent
   — V7); CI <15-min on every PR **unproven** (workflow exists; no timing/red-green
   evidence attached).
2. **Backend full suite: 557 tests, 12F+1E (V1).** Clusters: seed-agent count 4≠5
   (stale vs. gemini removal), hardcoded `claude-sonnet-4-6` expectation (stale vs.
   #104 lineup), timezone/DST drift. #120 fixed the five-module baseline (verified
   245-green minus one) but the WIDER suite has drift → wave-2 task.
3. **Odin suite: 23F/1010P (V2).** 11 = summarize fixture (known, #120 residue);
   12 = trace/streaming/taskit-mcp stale contracts predating the F29 live-trace
   rework. Long-standing, tracked — not new regressions.
4. **#120 parked-FAILED after its core landed** (merge `813b1264`): the two residual
   items exposed the **confined-loop platform gap** — a macOS-host-only failure
   passes inside the Linux guest, so three agent attempts truthfully "verified"
   success the host contradicts. Structural fix required (host-side verification
   step), not more attempts.
5. **Token accounting**: task metrics show totals (e.g. #119 ≈ 24.0M tok) but every
   spot-checked task flags "No token usage captured" for some executions —
   cost data is partial (feeds Track 10).

## Metrics snapshot (2026-07-06)

| Metric | Value | Note |
|---|---|---|
| Autonomy rate | **17/18 (94%) agent-authored board merges** | only #104 was hand-completed (pre-directive); 4 merge conflicts hand-resolved by operator (F24) — now automated by #115 |
| Time-to-verified | exec 3.5–30 min/task; dispatch→DONE same-day | wave of 10 follow-ups closed in ~30h wall including overnight host-sleep losses |
| Interrupts per spec | 20 findings (F31–F50), ~12 operator unsticks on spec #49 | each finding structurally encoded; 6 became product tasks |
| Cost per merged change | partial data (e.g. #103 10.5M, #114 5.4M, #119 24M tok) | capture gaps flagged; Track 10 baseline still open |
| Satisfaction fraction / Comprehension SLO / token-day | not measured | unchanged, per plan (M2–M3) |

## Risks (top 3)

1. **Test-suite drift erodes the verification signal** — stale expectations fail
   honest agents and train reviewers to distrust suites. *Mitigation:* wave-2
   reconciliation tasks with expectations derived from single sources
   (`agent_models.json` via `pricing.get_active_agents`), split per module.
2. **Platform-split verification gap** — agents can only prove what the Linux guest
   shows; macOS-host-only failures are invisible (killed #120's residue).
   *Mitigation:* host-side verification step in the promotion gate for
   platform-tagged tests; longer-term a macOS runner.
3. **Run/actor integrity** — F49 (unexplained main-tree edits, quarantined) and
   reflection VMs mounting the repo root rw; F48 fixed run-token checks in code but
   the live executor loads it only at next restart. *Mitigation:* wave-2 task:
   reviewers get read-only mounts; restart services to load #119; investigate F49
   patch authorship before deciding its fate.

## Ladder

**Proven: L1** (tests/coverage/doc-class work ships autonomously; reflection loop
enforced a user directive with zero operator hands — #114 qwen rework).
**L2 evidenced but not claimable:** the #113–#119 wave WAS scoped bug fixes
end-to-end with reflection gates, live-verified — but L2's unlocking tracks are
incomplete (02/03 unstarted, 01.2 parked, 07 Linux proof open). Blockers to L2
claim: harness conformance suite (02.x), failure taxonomy (03.x), lint gate, Linux
sandbox proof.

## Next actions (wave-2 draft, ranked)

1. **Reviewer/reflection mount hardening** — read-only repo mounts for reflection
   VMs; F49 investigation (integrity). *glm*
2. **Test-suite drift reconciliation, backend** — seed counts, lineup expectations
   from single source, timezone fix (V1's 13). *minimax*
3. **Test-suite drift reconciliation, odin** — trace/streaming/taskit-mcp stale
   contracts + summarize fixture (V2's 23). *minimax*
4. **#98 split** — three config-only tasks: ruff+config, prettier/eslint config,
   pre-commit hook wiring; no repo-wide autofix in-task. *glm*
5. **Host-side verification step** — promotion gate runs platform-tagged tests on
   the macOS host (closes the #120 gap). *minimax + operator hook*
6. **Bake python test deps into odin-agents image** — kills per-run pip toil
   (repeat occurrence). *glm + operator snapshot step*
7. **CI proof completion** — red/green PR evidence + timing <15 min (01.1 verify).
   *codex (quota window permitting) or minimax*
8. **seedmodels prune + agent seed sync** — DB retains qwen/gemini agent rows;
   prune on seed, single source. *minimax*
9. **UI: surface dispatch_blocked_reason + reflection live trace** (UI-first
   directive; #118 wrote the metadata, UI must show it). *minimax*
10. **Linux microsandbox proof** → then forkd removal (07.x gate). *operator +
    codex*

## Wave-1 findings digest

F31–F50 logged in OPERATIONS.md this wave; standouts: F44 (silent project-root
execution — guardrails now live and verified), F45 (quota-classifier sentinel →
dead-provider rework; fixed + live-verified), F46 (worktrees base on origin —
push-after-commit rule), F48 (zombie deadline killed healthy run; run-token check
shipped in #119), F50 (venv sweep; gitignore + cleaned tip). Four F24-class merge
conflicts were hand-resolved before #115's merge agent landed; zero after is the
wave-2 expectation.
