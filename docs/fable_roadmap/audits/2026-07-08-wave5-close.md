# Audit — wave 5 close

**Verdict: ON TRACK — wave 5 shipped 22/22 tasks plus a 2-task both-modes
smoke, all resting on the TESTING shelf per the new semantics. Full gate
GREEN on the reconciled branch (odin 1470+, backend 1022, frontend 194,
snapshots 43). The wave rebuilt the middle of the machine mid-flight on the
user's directives: promote-check retired, conflicts explained and resumed
from a human reply, runs supervised with kill-and-requeue, proof moved from
repo to board, merges tracked like reflections, and merges now land in ~1.5s
instead of 12 minutes.**

## Verified done (proof)

Everything was verified live during operation (promote evidence, suite runs
at each reconcile, live smoke both modes at close). Audit spot-checks on the
four features not personally exercised:

| Task | Proof |
|---|---|
| 206 explain-both-sides | merge_agent.py:999 `_side_why(Spec/Task)` + collision paragraph; unit tests |
| 207 reply resumes merge | signals.py:130-149 human-author guard → `resume_merge_with_guidance`; tests incl. agent-comment-no-trigger |
| 215 disk pruning | microsandbox.py run-exit `shutil.rmtree` + reconciler sweep; mock tests |
| 216 proof to board | proof_upload.py attachments + worktree.py `.proof` merge exclusion + gitignore; tests incl. truncation marker |

Everything else: 201/202 twins+quote (merged, conflict resolved per user
guidance), 204 reviewer re-eval (acted on live — reviewer flipped to glm-5.2
during the token incident and validated on real reviews), 205/208 merge flow
(exercised by every merge since), 210/211 run supervision, 213 doctor, 212/
217 audit presets, 214 dep gate, 218 story page, 219/220 smoke (sandboxed
and unsandboxed table creation, both green end to end).

Demotions: none. One scope amendment (205: TESTING stays the resting shelf,
DONE is the human's flip — user directive mid-wave).

## What the wave cost the human (question 1)

Real operator interventions: 3 merge conflicts resolved by hand (201, 218,
and 216's network-timeout retry — 207's reply-resume now exists because of
exactly this), 2 reflection kicks after the host slept, 1 agent reassignment
during the claude-token window. Root causes all became structure: W6.4
(executor-owned reflections), the error ledger, pmset guidance, and the
Linux-box evidence file. The stall alarm now catches what the user caught
manually once (201's unheard comment).

## Metrics snapshot

- 24/24 tasks landed (22 wave + 2 smoke); hand-counted **19-20 of 24 fully
  hands-free** (wave 4: 0 of 10). `autonomy_metrics.py` cannot see this —
  it counts only DONE and the shelf rests at TESTING; updating it for the
  new semantics is filed work (finding).
- Merge latency: 12 min → **1.5 s** (dedicated queue, measured live).
- Disk: 183 GB of dead sandbox images found and freed; prune-on-exit shipped.
- Board total 564M tokens across 9 specs.
- Learned twice? No — every incident this wave produced its structural fix
  in the same wave or the wave-6 loader, and the error ledger now catches
  the residue.

## Bucket moves (question 2, evidence named)

| Bucket | Move | Evidence |
|---|---|---|
| Trust | 7→8 | kill-and-requeue supervision live; honest merge flow exercised by 20+ real merges; dep gate; disk hygiene; both-modes smoke |
| Memory | 2→4 | twins + quote merged with tests and real-board examples; not yet observed steering a live dispatch — score waits there |
| Routing & cost | 3→4 | 204's eval acted on in production (glm-5.2 reviews live, cheaper, validated); routing table merged |
| Watching | 3→4 | story page + merge ledger + stall alarms; changelog duty still manual (story page should eat it) |
| Human's seat | 2→3 | conflicts ask an answerable question; your reply finishes the merge; changelog artifact |
| Audits | 2→3 | preset contract + three presets incl. static-autolint |
| Ease of use | 1→2 | doctor shipped; quickstart is wave 6 |
| Getting oriented / Project powers / Moonshots | — | no movement |

## Top 3 risks

1. **The new machinery is one restart old.** Auto-flows (reply-resume,
   reconciler, proof upload) have tests but thin live mileage. Mitigation:
   wave 6 runs on top of them; first week is the real test.
2. **Host sleep remains lethal** — froze the pipeline for 6 hours; caffeinate
   is a band-aid, pmset needs the user's sudo, the Linux box remains the
   only real fix (decision open, evidence now overwhelming).
3. **glm-5.2 reviewer at scale** — one prose-no-verdict ERROR seen at N=1
   live; W6.4's retry-with-fallback covers it, but watch the rate.

## Ladder

Level 1 holds; Level 2 (10 hands-free bug fixes in a row) now measurable
and plausibly near: 19-20 hands-free landings this wave, but L2 counts
bug-fix tasks specifically and needs autonomy_metrics updated for shelf
semantics before an honest claim. Update the counter, then judge at the
wave-6 audit.
