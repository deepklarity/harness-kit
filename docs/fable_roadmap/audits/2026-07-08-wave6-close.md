# Audit — wave 6 close

**Verdict: ON TRACK — wave 6 shipped 15/15, all shelf-resting, gate GREEN
(odin 1470+, backend 1253, frontend 217, snapshots 43). The wave grew from
5 planned tasks to 15 by absorbing live feedback and incidents as work —
which is the operating model functioning, not scope creep. Agent-authored
merges: 14 of 15 (wave 5: 18 of 22; wave 4: 0). The reflections and merges
queues both run dedicated workers now.**

## Verified done (proof)

Code-verified with tests by audit agent (11) + exercised live (4):

| Landed | Evidence |
|---|---|
| 221 executor-owned reflections | dag_executor.py:3228 scan_pending_reflections + reflections queue worker live post-restart |
| 222 ErrorEvent ledger | models.py:649 + errors.py triage surface; manual doc imported |
| 223 mistakes file themselves | models.py:601 MistakeEntry + twins warnings |
| 224 quickstart | QUICKSTART.md + provider-agnostic sample loader |
| 225 failure fingerprints | fingerprint on MistakeEntry + replay tool; merged via reply-resume saga (below) |
| 226 honest L2 counter | LANDED_STATUSES incl TESTING + --ladder; run at close: streak 0 |
| 227 warm starts | orchestrator.py:693 records suggested docs into prompts + metadata |
| 228 league table | league_table.py + /league/ API + Members UI (retry after sleep kill) |
| 229 board story | board_story.py build_board_story — the changelog's future generator |
| 230 token profiler | token_profile.py find/do/check phases |
| 231 one waiting-on-human flag | migration-gate parks as needs_human; retryable classes enumerated |
| 232 doctor stale-token WARN + resume on merges queue | (landed in final reconcile) |
| 233 twins in the task modal | SimilarTasks.tsx, structured data, new-tab links |
| 234 assignment WHY | routing-history reason stamped + modal surface (retry after parse failure) |
| 235 progress-based liveness | _run_progress_mtime — zombies with live pids now die |

Demotions: none.

## What the wave cost the human (question 1)

The 225 merge saga: reply-resume fired correctly on the first live conflict
but couldn't hear it (flag mismatch → 231) and couldn't execute a branch
edit (→ scope in 231), so the operator merged by hand — three attempts
because branch surgery raced the worktree-first flow. Host slept twice more
(zombie sandboxes → 235 merged; watcher clock lies → CLOCK_JUMP detection
shipped in-session). One agent parse failure (234) required a worktree
cleanup + requeue — retry-cleans-worktree is a known small gap, not yet
filed. Everything else: hands-free.

## Metrics snapshot

- 15/15 landed; agent-authored merges 14/15; operator touches 40
  (2.7/task; wave 5: 2.6). Zero-unstick now machine-counted.
- L2 ladder (new, honest): 7 fix-class tasks ever landed, 0 hands-free,
  streak 0. L2 is genuinely far; the counter ends the guessing.
- Board: 10 specs, 125 tasks, ~600M tokens.
- Learned twice? The host slept THREE times across two waves before the
  full mitigation set existed (caffeinate discipline + CLOCK_JUMP + zombie
  liveness). The machine now survives sleep; only the Linux box removes it.

## Bucket moves (question 2, evidence named)

| Bucket | Move | Evidence |
|---|---|---|
| Memory | 4→5 | mistakes ledger + fingerprints + error ledger live; twins/quotes now steer briefs (twins comments on all wave-6 tasks) |
| Getting oriented | 4→5 | warm starts in every prompt + token profiler to judge them |
| Watching | 4→5 | league table, board story, modal twins, profiler — data now has surfaces |
| The human's seat | 3→4 | assignment WHY, twins modal, reply-resume proven live (with its gaps fixed same-wave) |
| Routing & cost | 4→5 | league table visible; assignment reasoning surfaced; reviewer on glm-5.2 validated at scale (~25 reviews) |
| Ease of use | 2→3 | quickstart shipped; doctor hardened |
| Trust | 8 (hold) | reflections owned + zombie liveness are strong, but the wave also proved sleep still breaks flow twice — the point comes when an unattended sleep round-trips with zero operator touches |
| Audits / Project powers / Moonshots | — | no movement |

## Top 3 risks

1. **Host sleep remains the dominant failure source** — three incidents,
   ~7 lost hours across two waves. Every mitigation is now in place except
   the actual fix. The Linux box is no longer a preference; it is the next
   point of Trust.
2. **New surfaces unmeasured** — warm starts, twins-steering, assignment
   WHY shipped but nobody has measured behavior change (do agents read the
   suggested docs? does finding-share drop?). The profiler exists precisely
   for this; wave 7 should run the measurement.
3. **L2 distance** — streak 0 because fix-class tasks are rare and never
   hands-free yet. Wave 7 should route real bug fixes through the machine
   deliberately.

## Ladder

L1 holds. L2 counter now machine-computed: 0/10. The path: feed it real
fix-class tasks and let the wave-6 machinery land them untouched.
