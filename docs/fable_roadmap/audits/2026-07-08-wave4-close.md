# Audit — wave 4 close

**Verdict: ON TRACK — wave 4 shipped 10/10 tasks (one rejected post-merge and
reverted), reconciled into `feat/forkd-harness-toggle`, full verify gate GREEN
(odin 1499, backend 871, frontend 183, snapshots 43). The wave's theme —
eating operator duties — landed: auto-promotion, auto-requeue, per-task proof
paths, CANCELED status, merge watchdog, and an honest env-missing gate are all
live after the service restart.**

## Verified done (proof)

| Task | Check | Result |
|---|---|---|
| 185 per-task proof paths | `promote_check.py:56` `default_proof_artifact` + `orchestrator.py:3364` write per-task dirs; mock tests | verified in code + tests |
| 186 auto-promotion v1.5 | `tasks/auto_promote.py:408` celery task flips green TESTING→DONE; `test_auto_promote.py` incl. green/hold/error cases | verified in code + tests |
| 187 wave-draft script | REJECTED post-merge (duplicates `odin plan`); reverted at wave close, proof kept under `.proof/task-187/` | demotion, handled |
| 188 data-driven routing | promote-check: odin/backend/snapshots PASS, reflection PASS; round-2 fixed the orchestrator full-pool gap + regenerated artifacts | promoted by operator |
| 191 reviewer re-eval | `odin/tests/reflection_eval/eval.py` harness + `.proof/task-191/proof.md`; honest verdict: N too small to flip the sonnet default yet | verified; follow-up = live N≥5 run |
| 192 CANCELED status | model + migration 0049 + 26 tests + frontend classification/color/icon | verified in code + tests |
| 193 merge-flag reconciliation | promote-check PASS ×3 suites; `is_ancestor` helper + reconciliation pass merged | promoted by operator |
| 194 auto-requeue | promote-check PASS ×3 suites, reflection PASS | promoted by operator |
| 195 honest verify gate | after operator fixes (exec bit, 0050 migration) gate itself proved the feature: frontend reported ENV_MISSING instead of a fake FAIL | promoted by operator |
| 196 merge watchdog | promote-check PASS ×3 suites; `is_task_merged` git-reality check merged | promoted by operator |

Demotions: 187 (rejected + reverted). No other claims downgraded.

## What the wave cost the human (question 1)

The five finishing tasks sat ~5 hours in REVIEW/TESTING waiting for an
operator: two on `.proof/proof.md` merge conflicts (tasks created before the
W4.1 convention — transitional, should not recur), three on promote-check
holds that were all false gaps. Hand-fixes done today, each now a filed task:

1. promote-check parses the brief's literal `.proof/task-<id>/` placeholder as
   an artifact path → false MISSING on every task. Extractor must substitute
   the real id or skip placeholder paths.
2. Task 195's commit dropped the exec bit on `scripts/verify.sh` →
   promote-check crashed with PermissionError. Gate should invoke via `sh`
   instead of trusting the mode bit.
3. Two same-wave tasks minted migration `0049_*` → conflicting leaves broke
   the backend suite on sibling branches. Gate (or brief template) should run
   `makemigrations --check` against the spec branch before merge.
4. Frontend suite red on every task worktree until 195 landed — now reported
   honestly as ENV_MISSING; actual provisioning (node_modules in worktrees)
   is still open.

## Metrics snapshot (`autonomy_metrics.py`)

- Wave 4: 10 DONE, 44 operator touches (4.4/task, wave 3 was 5.25), 127M
  tokens. Agent-authored merges 0/10 — merges still land via the executor
  path without trailers; zero-unstick 0/89 all-time.
- Board total 453M tokens, 5 historical cost-capture gaps (pre-fix, known).
- Learned twice (question 3): no — the proof-collision lesson became W4.1,
  the false-red gate lesson became W4.11, both shipped in the same wave.

## Bucket moves (question 2)

- **Trust 6→7**: gate now names ENV_MISSING instead of lying (195), watchdog
  verifies git reality before crying wolf (196), CANCELED ends the
  DONE-or-FAILED lie (192). Evidence: promote-check output before/after 195.
- Others unchanged; routing (188) shipped mechanism but its table needs live
  history before the score moves.

## Top 3 risks

1. **Auto-promotion runs unwatched for the first time in wave 5.** The false
   gaps found today (placeholder artifacts) would have held every green task
   — fix items 1–2 above before trusting it. Mitigation: they are the top of
   the backlog.
2. **Migration-number collisions inside a wave** will recur as backend tasks
   parallelize. Mitigation: gate check (item 3).
3. **Host limits unchanged** — one 4 GB sandbox, sleeping laptop. Linux
   machine decision still open, still the biggest lever.

## Ladder

Level 1 holds. Level 2 (10 hands-free bug fixes in a row) not started:
zero-unstick counter is 0 — every wave-4 task needed at least one operator
touch. The wave-4 automations exist precisely to move this; wave 5 is the
first wave that can score.
