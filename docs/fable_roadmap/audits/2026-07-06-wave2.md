# Fable audit — 2026-07-06 (wave 2 close)

**Verdict: ON TRACK** — wave 2 closed 18/18 tasks agent-authored and merged
(11 scoped fixes + 7 provider smokes), all five providers proven confined
end-to-end, both lineages reconciled to one head; L2 remains unclaimable because
the zero-unstick streak (7/11, non-consecutive) and the local verification gate
don't exist yet.

Auditor: operator session (Claude). Scope: everything since `2026-07-06.md`
(wave-1 audit) — spec #52 (wave 2), specs #53/#54 (smoke), the close-out session
work. Board state at audit: **40/40 DONE** across specs #49/#51/#52/#53/#54.

## Verified-done table

Every item verified against the merged lineage `b4019b4e`
(`feat/forkd-harness-toggle` ≡ `spec/sp_fable_w2`, pushed). "Suite" =
executed at audit time on this host.

| Item | Proof |
|---|---|
| #127 reviewer read-only mounts | commit `65c12667`; reflection mounts `:ro`, exec `:rw`; 71 harness tests green incl. `test_reflection_run_mounts_every_repo_path_read_only` |
| #128/#129 test-drift reconciliation | backend suite **597 OK**, odin suite **1147 passed** on the merged lineage (run this session); the lineage merge alone fixed 10 pre-existing backend failures |
| #130–#132 ruff / prettier+eslint / pre-commit | `ruff.toml`, `.prettierrc`, `.prettierignore`, `.pre-commit-config.yaml` on the operator mainline via merge `61f6b753` |
| #133 image test deps | `bootstrap/sandbox_image/extend_image_test_deps.sh` — real 72-line recipe with import-verification step, matches sibling recipes |
| #135 seedmodels prune | 4 prune tests green in the 597 (after host-portability fix `2607b728` — see divergences) |
| #136 dispatch-blocked UI | `DispatchBlockBanner.tsx` + `dispatchBlock.ts`; 6 TaskCard tests green covering all four block reasons + fallback |
| #137 auto-redispatch | `tests.test_infra_auto_redispatch` green from the spec branch (part of 42/42 spot-check); **live in the executor since the 2026-07-06 service restart** |
| #138 worktree auto-prune | `tests.test_dag_executor_worktree` green (42/42 spot-check); scope caveat below |
| #142–#148 smoke (5 providers + concat) | exact table artifacts verified per-file on `spec/sp_agy_smoke` / `spec/sp_smoke_all_v2`, concat correctly sorted; **agy confined e2e proven** (host-keychain→guest-keyring, commit `92a4bf4c`) |
| Lineage reconciliation (F40) | one head `b4019b4e` on both branches, pushed to origin; suites run post-merge |

## Demotions and divergences

1. **#138 is narrower than its title.** `_cleanup_done_task_worktree` fires only
   on the spec-finalize path; the operating promotion flow (API PATCH → DONE)
   bypasses it, so worktrees were pruned by hand this session. Not demoted —
   the code does what its tests claim — but the duty is only *partially*
   retired. Wiring filed into BACKLOG READY 2.
2. **#135's tests were guest-only-correct**: fixtures written to a hardcoded
   `/tmp/opencode/` path that exists in the agent sandbox, not on the macOS
   host (the #120 platform class). Fixed at audit (`2607b728`). Reflection had
   PASSed it — evidence that reviewer verification inherits the guest's
   platform blindness. Feeds BACKLOG 11 (host-side platform verification).
3. **Token capture regressed to zero.** Wave 1 had partial token data; wave 2
   tasks have none (`metadata.usage` empty everywhere, spec_trace shows "-").
   The 5 snapshot cost tests are known-red for the same cluster (golden
   predates the #135 lineup prune; fresh capture impossible without usage
   data — proven by a failed re-capture attempt from #54, reverted). Filed as
   BACKLOG READY 13.
4. **Obsolete CI contract tests deleted** (`test_ci_workflow_contract.py`,
   6 tests): they asserted the workflow that user directive `64ab8451`
   removed. Successor contract = local verification gate (BACKLOG 1).
5. **PROGRESS.md ↔ board divergence**: PROGRESS still said "#137/#138 TESTING
   awaiting promotion" — stale after this session's promotions. Fixed in this
   audit's tracker update (board is truth).

## Metrics snapshot (hand-computed from board data; script = BACKLOG 7)

| Metric | Wave 1 | Wave 2 + smoke (this audit) |
|---|---|---|
| Agent-authored merges | 17/18 (94%) | **18/18 (100%)** |
| Tasks dispatch→TESTING with zero operator unsticks | not measured | **10/18** (wave-2 subset: 7/11) |
| Operator unstick transitions | ~12 | **~8** (concentrated in 6 tasks; worst: #135 ×5 attempts, #131 ×3) |
| Exec duration | 3.5–30 min | **55 s – 19.4 min** |
| Token/cost per merged change | partial capture | **none captured** (regression — READY 13) |
| Suite health at close | drifting (13+23 failing) | **odin 1147 ✓ · backend 597 ✓ · frontend 141 ✓ · snapshots 38/43** (5 known-red, filed) |

## Risks (top 3)

1. **The verification signal is still assembled by hand.** Four suites, three
   directories, one known-red pocket, no single command — autonomy claims rest
   on the operator remembering to run everything. *Mitigation: wave 3 leads
   with BACKLOG 1 (local gate); wire it into promotion (READY 2) and this
   audit protocol.*
2. **Unstick concentration.** 6 of 18 tasks consumed ~8 operator interventions;
   #137's auto-redispatch went live only at session end and has zero production
   runs. *Mitigation: run wave 3 with #137 live and measure its
   classification hit-rate; failure tagger (READY 4) extends it.*
3. **Cost blindness.** With zero token capture there is no cost-per-change
   trend, and the pricing registry breaks on retired models. *Mitigation:
   READY 13 scoped; keep it in the wave-3 draft.*

## Ladder

- **L1 proven** (2026-07-05, unchanged).
- **L2 evidenced, not claimable.** Wave 2 was exactly L2-shaped work (scoped
  fixes end-to-end) and merge autonomy hit 100%, but the exit criteria demand
  10 *consecutive* zero-unstick tasks on a *locally green gate* with failures
  *auto-classed*. Today: best streak <10, gate absent (BACKLOG 1), #137 live
  but unproven. **Blockers: BACKLOG 1 + a clean wave-3 streak.**
- **L3 blocked** on planning entry (constraint 4) and promotion assistant
  (READY 2); throughput (constraint 3) is a standing user decision.

## Ratchet check (plan-v2 rules)

| Rule | Status |
|---|---|
| Hand-cranks filed same day | ✓ (READY 2 amendment, READY 13, promotion-path gap — all filed in-session) |
| Wave retires ≥1 operator duty | ✓ #137 retires infra-failure triage/redispatch; #138 retires pruning on the finalize path only (partial) |
| P1+ findings have structural fixes | ✓ F53 fixed+tests; F54 → READY 5; F55 → rule + open DECISION 3; F56 → #138 + preflight |
| Merged tasks carry tests + evidence | ✓ spot-checked #127/#133/#136/#137/#138; smoke tasks are evidence-by-artifact |
| Brief quality compounds | ✓ create_wave2.py checklist held (zero F43/F44/F45-class misfires this wave) |
| Metrics from scripts | ✗ still hand-counted — this table says so; READY 7 queued |

## Backlog re-rank

`BACKLOG.md` updated in place this session: NOW collapsed (all four items
executed), READY 13 added (token capture + retired-model pricing), READY 2
gained the DONE-path worktree-reclaim wiring. **Wave-3 draft = READY 1
(local gate) → READY 2 (promotion assistant) → READY 13 (token capture) →
READY 3 (agy MCP) → READY 4 (failure tagger).** Rationale: 1 unblocks honest
gates everywhere (and the L2 claim); 2 retires the biggest operator duty and
mounts 11; 13 restores the cost signal; 3 closes the last provider gap; 4
compounds #137. No entries retired as stale.

## Session evidence trail

Commits `92a4bf4c` (agy fix + tests) · `2248559f` (plan v2 docs) · `61f6b753`
(lineage merge) · `2607b728` (suite fixes + CI-test deletion) · `b4019b4e`
(close-out docs). Session narrative: `audits/session_history.md` (2026-07-06
late-night block).
