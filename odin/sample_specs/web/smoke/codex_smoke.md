# Codex Smoke Test

Create `smoke_output/codex.html` — a single HTML page with a styled data table (4 columns: Name, Role, Score, Notes; 3 rows of fictional data). Use inline CSS with a red color theme. Include a heading "Codex Smoke Test".

## Tasks

1. **Codex table** — Agent: codex. Create the HTML file as described above.
