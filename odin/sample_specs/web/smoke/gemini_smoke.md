# Gemini Smoke Test

Create `smoke_output/gemini.html` — a single HTML page with a styled data table (4 columns: Name, Role, Score, Notes; 3 rows of fictional data). Use inline CSS with a green color theme. Include a heading "Gemini Smoke Test".

## Tasks

1. **Gemini table** — Agent: gemini. Create the HTML file as described above.
