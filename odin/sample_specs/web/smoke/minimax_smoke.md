# MiniMax Smoke Test

Create `smoke_output/minimax.html` — a single HTML page with a styled data table (4 columns: Name, Role, Score, Notes; 3 rows of fictional data). Use inline CSS with an orange color theme. Include a heading "MiniMax Smoke Test".

## Tasks

1. **MiniMax table** — Agent: minimax. Create the HTML file as described above.
