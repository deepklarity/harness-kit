# GLM Smoke Test

Create `smoke_output/glm.html` — a single HTML page with a styled data table (4 columns: Name, Role, Score, Notes; 3 rows of fictional data). Use inline CSS with a teal color theme. Include a heading "GLM Smoke Test".

## Tasks

1. **GLM table** — Agent: glm. Create the HTML file as described above.
