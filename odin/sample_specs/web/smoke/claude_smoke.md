# Claude Smoke Test

Create `smoke_output/claude.html` — a single HTML page with a styled data table (4 columns: Name, Role, Score, Notes; 3 rows of fictional data). Use inline CSS with a purple color theme. Include a heading "Claude Smoke Test".

## Tasks

1. **Claude table** — Agent: claude. Create the HTML file as described above.
