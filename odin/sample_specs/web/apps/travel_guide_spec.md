Create a small collaborative HTML travel guide about Paris.

Each agent writes one section (Overview, Attractions, Food, Transport, Budget, Fun Facts) as an HTML `<section>` with a heading and a short paragraph. Include the agent's name in a `<mark>` tag.

Sections are written sequentially. Each agent writes to `<agent_name>.html` first, then a final agent combines everything into `mini_travel_guide.html` with basic CSS styling.

Create final output in test2/
